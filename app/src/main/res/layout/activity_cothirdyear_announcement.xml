<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@color/background_light">

    <!-- Header -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="3rd Year CO Notifications"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/black"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Modern ListView with notification items -->
    <ListView
        android:id="@+id/listViewNotifications"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:divider="@android:color/transparent"
        android:dividerHeight="12dp"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <!-- Sample notification items (for reference) -->
    <TextView
        android:id="@+id/sampleNotification1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="• Class test postponed to Friday"
        android:textSize="16sp"
        android:padding="12dp"
        android:background="@drawable/notification_bg"
        android:layout_marginBottom="8dp"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"/>

    <TextView
        android:id="@+id/sampleNotification2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="• Project submission deadline extended"
        android:textSize="16sp"
        android:padding="12dp"
        android:background="@drawable/notification_bg"
        android:layout_marginBottom="8dp"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/sampleNotification1"/>

    <TextView
        android:id="@+id/sampleNotification3"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="• Important: Lab schedule changed"
        android:textSize="16sp"
        android:padding="12dp"
        android:background="@drawable/notification_bg"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/sampleNotification2"/>

</androidx.constraintlayout.widget.ConstraintLayout>